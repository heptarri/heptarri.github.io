import{_ as n,o as i,c as e,ak as s,aC as t,aD as l,aE as p,aF as r}from"./chunks/framework.DXL0ymeD.js";const g=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"AUTOSAR Practice/MCAL 模块详解/Uart.md","filePath":"AUTOSAR Practice/MCAL 模块详解/Uart.md"}'),o={name:"AUTOSAR Practice/MCAL 模块详解/Uart.md"};function c(d,a,_,U,u,R){return i(),e("div",null,[...a[0]||(a[0]=[s('<p>注：配置 Uart 模块需要同步添加 Platform 模块用于 Async 中断配置、Mcu 模块用于时钟配置、Port 模块用于输出端口配置。</p><p>在 Module Configuration 中添加 Mcu、Port、 Uart 和 Platform 模块。</p><p>打开 Uart 模块，在 General 中选择 Post Build Variant Build，Config Variant 中同步设置 VariantPostBuild。该步的设置使在代码中设置 Uart_Init 时需要手动指定配置结构体（见代码）。</p><p>关于 UartClockRef 时钟参考点即时钟配置，需要在 Mcu 模块中配置：</p><p>在Mcu -&gt; McuClockSettingConfig 中新建并进入。</p><p>启用 SOSC、SIRC、FIRC、PLL，并在 PeripheralClockConfig （时钟外设）中开启 LPUART1 时钟为 SPLL。</p><p>在 ClockReferencePoint 中添加 Point 为 McuClockReferencePoint_LPUART （时钟参考点）为 LPUART1_CLK。</p><p>关于输入输出端口需要在 Port 中配置：</p><p>添加 PortPin：CH340_RX、CH340_TX，并分别设置为 LPUART1_RX、LPUART1_TX，其他设置如图所示：</p><p><img src="'+t+'" alt=""></p><p><img src="'+l+'" alt=""></p><p>接下来是 Uart 模块配置。</p><p>新建 Uart Channel。在 UartHwUsing 中选择 LPUART_IP，Channel ID 设置为 0，该 ID 在代码中用于指定外设。</p><p>Uart hardware channel 可以设置为任意，但 Channel ID 始终为上面设置的值(0)。</p><p>DesireBardrate 设置为 115200。</p><p>Uart Asynchronous Method 设置为 USING INTERRUPTS，即使用中断异步而非 DMA。</p><p>其他设置如图所示：</p><p><img src="'+p+'" alt=""></p><p><img src="'+r+`" alt=""></p><p>为配置中断，需要在 Platform 模块中配置中断。在 Interrupt Controller 新建一个并进入，进入 Interrupt Configuration 页，找到 LPUART1_RxTx_IRQn，启用 Interrupt Enabled 选项，并设置优先级。</p><p>在 Platform -&gt; Generic Interrupt Settings 中，找到 LPUART1_RxTx_IRQn，选择启用（M4），并设置相应 IRQ Handler：<code>LPUART_UART_IP_1_IRQHandler</code>。</p><p>该函数名可以在 RTD 中 Uart -&gt; src -&gt; Lpuart_Uart_Ip_Irq.c 中找到（Line 142）：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>#if (LPUART_UART_IP_NUMBER_OF_INSTANCES &gt; 1U)</span></span>
<span class="line"><span>#ifdef LPUART_UART_IP_INSTANCE_USING_1</span></span>
<span class="line"><span>/* Implementation of Lpuart1 handler named in startup code. */</span></span>
<span class="line"><span>ISR(LPUART_UART_IP_1_IRQHandler)</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>    Lpuart_Uart_Ip_IrqHandler(1U);</span></span>
<span class="line"><span>    EXIT_INTERRUPT();</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>#endif</span></span>
<span class="line"><span>#endif</span></span></code></pre></div><p>至此，在 EB Tresos 中的所有关于 Uart 的配置全部完成。进入 S32DS，开始代码配置：</p><p>主要代码如下所示：</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Uart_Init(&amp;Uart_xConfig);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Uart_AsyncSend((uint8)0U, (uint8 *)log, (uint32)size);</span></span>
<span class="line"><span>Uart_SyncSend((uint8)0U, (uint8 *)log, (uint32)size, 10000);</span></span></code></pre></div><p>需要注意的是，Send 函数的第一个参数的值即为 Channel ID 的值。</p><p>代码注释：</p><p>UART（通用异步收发传输器）通信接口操作，主要实现了 UART 的初始化和数据发送功能，具体解析如下：</p><ol><li><code>Uart_Init(&amp;Uart_xConfig);</code></li></ol><ul><li>功能：初始化 UART 外设，配置通信参数。</li><li>参数：<code>&amp;Uart_xConfig</code> 是一个 UART 配置结构体的指针，结构体中通常包含： <ul><li>波特率（如 9600、115200 ）</li><li>数据位（如 8 位）</li><li>停止位（如 1 位）</li><li>校验位（如无校验、奇校验、偶校验）</li><li>硬件流控设置（如是否启用 RTS/CTS）等。</li></ul></li><li>作用：通过该函数完成 UART 外设的底层配置，使其满足特定的通信协议要求，为后续数据收发做准备。</li></ul><ol start="2"><li><code>Uart_AsyncSend((uint8)0U, (uint8 *)log, (uint32)size);</code></li></ol><ul><li>功能：异步发送数据（非阻塞方式）。</li><li>参数解析： <ul><li><code>(uint8)0U</code>：UART 通道号（通常一个 MCU 可能有多个 UART 外设，此处指定使用第 0 号 UART）。</li><li><code>(uint8 *)log</code>：待发送的数据缓冲区指针（将<code>log</code>指针转换为<code>uint8*</code>类型，指向要发送的字节数据）。</li><li><code>(uint32)size</code>：待发送的数据长度（以字节为单位，转换为 32 位无符号整数）。</li></ul></li><li>特点：调用后立即返回，数据发送过程在后台（通常通过中断或 DMA）完成，不会阻塞当前程序执行流程。适用于对实时性要求高、不希望等待发送完成的场景。</li></ul><ol start="3"><li><code>Uart_SyncSend((uint8)0U, (uint8 *)log, (uint32)size, 10000);</code></li></ol><ul><li>功能：同步发送数据（阻塞方式）。</li><li>参数解析：前三个参数与<code>Uart_AsyncSend</code>一致，最后一个参数<code>10000</code>是超时时间（单位通常为毫秒或系统滴答数）。</li><li>特点：调用后会阻塞当前程序，直到数据发送完成，或等待时间超过超时时间后返回。适用于需要确保数据发送成功后再执行后续操作的场景（如关键指令传输）。</li></ul><h3 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to “总结”">​</a></h3><p>这组代码的典型执行流程是：</p><ol><li>先通过<code>Uart_Init</code>完成 UART 的参数配置；</li><li>按需选择<code>Uart_AsyncSend</code>（非阻塞）或<code>Uart_SyncSend</code>（阻塞 + 超时控制）发送数据，实现与外部设备的串行通信。</li></ol><p>实际使用时，需确保<code>Uart_xConfig</code>结构体的参数与通信对方匹配（尤其是波特率、校验方式等），否则会出现数据传输错误。</p><p>至此配置完毕。</p>`,40)])])}const A=n(o,[["render",c]]);export{g as __pageData,A as default};
